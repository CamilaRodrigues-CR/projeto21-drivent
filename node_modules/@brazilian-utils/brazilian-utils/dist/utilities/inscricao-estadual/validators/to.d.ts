import { CE } from './ce';
export declare class TO extends CE {
    protected checkLength(ie: string): boolean;
    protected itStartsWith(ie: string): boolean;
    protected calcIe(ie: string): boolean;
    private checkOld;
    private oldStartsWith;
    private calcOld;
    private checkNew;
    private calcNew;
}
